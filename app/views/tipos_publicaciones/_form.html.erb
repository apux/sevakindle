<%= form_for(@tipo_publicacion) do |f| %>
  <%= render 'error_messages', target: @tipo_publicacion %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
